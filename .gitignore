# Rust
/target/
**/*.rs.bk
*.pdb
Cargo.lock

# Runtime logs
run.log
*.log

# Installer build artifacts (the .nsi recipe is tracked; the binaries are not)
/installer/HCAD-Setup.exe
/installer/vc_redist.x64.exe

# Local test parts / bug-repro files saved out of the app
/saved files/

# Raw source art / working screenshots (the ones used in the repo live under
# crates/hworks-app/assets and docs/screenshots; these folders are the untracked originals).
/logo/
/screen shots/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
fillet_dbg.txt
